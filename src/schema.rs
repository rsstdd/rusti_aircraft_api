table! {
    airplanes (id) {
        id -> Int4,
        name -> Varchar,
        description -> Varchar,
        year_in_service -> Varchar,
        country_of_origin -> Varchar,
        operators -> Varchar,
        max_speed -> Varchar,
        max_range -> Varchar,
        ceiling -> Varchar,
        engines -> Varchar,
        img_url -> Varchar,
    }
}
